1. Склонировать реп `git clone ...`
2. В каталоге репозитория сделать `npm i -g .`
3. Готово. Заходим в каталог задания и запускаем `add-files`. После этого проверяем `meta.json` и редактируем, как нужно.
